
export const add = (a: number, b: number): number => {
    return a + b
}
export const subtact = (a: number, b: number): number => {
    return a - b
}
export const multiply = (a: number, b: number): number => {
    return a * b
}
export const divide = (a: number, b: number): number => {
    return a / b
}